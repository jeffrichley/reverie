"""Tests for the reverie engine.

Every test here runs against a synthetic world. Fact types are `t1`/`t2` and
beings are `a`/`b`, deliberately: the README makes "engine tests may not contain
a domain noun" a rule, and this package is where it holds or erodes.
"""
